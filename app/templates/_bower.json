{
  "name": "<%= _.slugify(appname) %>",
  "version": "0.0.0",
  "dependencies": {
    "ember": "1.5.0",
    "handlebars": "1.2.1",
    "ember-data": "1.0.0-beta.7"<% if (compassBootstrap) { %>,
    "bootstrap-sass-official": "3.1.1"
    <% } %>
  },
  "resolutions": {
    "ember": "1.3.1"
  },
  "devDependencies": {<% if (testFramework === 'mocha') { %>
    "ember-mocha-adapter": "0.1.2"<% } %>
  }
}

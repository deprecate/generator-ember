<h1><%= name %></h1>

<table>
  {{#each model.attributes}}
  <tr>
    <td>
      <strong>{{this.key}}:</strong>
    </td>
    <td>
      {{this.value}}
    </td>
  </tr>
  {{/each}}
</table>

{{#link-to '<%= slugified_name %>.edit' model}}Change{{/link-to}}

{{outlet}}
